// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyBwdgkXES-Idd3-gtoMymPaho7_nJMyffs",
    authDomain: "camwin-87018.firebaseapp.com",
    projectId: "camwin-87018",
    storageBucket: "camwin-87018.appspot.com",
    messagingSenderId: "655313657325",
    appId: "1:655313657325:web:260518849ed68070005be4",
    measurementId: "G-13CVVZNNCW"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
